import React from "react";
import Model from "./Model";
import { data } from "../api";

const UseReducer = () => {
  return (
    <div>
      <h1>UseReducer</h1>
    </div>
  );
};

export default UseReducer;
